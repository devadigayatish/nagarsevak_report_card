<script>
function showUser(str) 
{
    if (str == "") 
    {
        document.getElementById("prabhag_no_info").innerHTML = "";
        return;
    } //prabhag_no_info is <div> inside the body
    else 
    {
          if (window.XMLHttpRequest) 
          {
            // code for IE7+, Firefox, Chrome, Opera, Safari
            xmlhttp = new XMLHttpRequest();
          } 
          else 
          {
            // code for IE6, IE5
            xmlhttp = new ActiveXObject("Microsoft.XMLHTTP");
          }
          
          xmlhttp.onreadystatechange = function() {
                                                      if (xmlhttp.readyState == 4 && xmlhttp.status == 200) 
                                                      {

                                                          document.getElementById("prabhag_no_info").innerHTML = xmlhttp.responseText;
                                                      }               //prabhag_no_info is <div> inside the body

                                                    };

            xmlhttp.open("GET","1.php?q="+str,true);
            //1.php is php file , q is value we are sending to php file by GET function
            xmlhttp1.send();
    }
}
</script>            